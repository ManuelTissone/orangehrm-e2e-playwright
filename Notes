npm init -y
#--save-dev (o su forma corta: -D)
#Entonces el comando completo sería:
npm install -D @playwright/test
#¿Ves node_modules? Esa carpeta contiene TODAS 
#las dependencias de Playwright (miles de archivos).
#¿Ves package-lock.json? Es un archivo que "congela" 
#las versiones exactas instaladas. Importante para reproducibilidad.

tests/
├── pages/ (Page Object Model)
├── fixtures/ (datos reutilizables)
├── e2e/ (casos de prueba)

- Fixtures:
En Playwright, fixtures/ tiene datos que reutilizas en múltiples tests:
#Usuarios (Admin, Employee, Manager)
#URLs base
#Datos de empleados para crear
#Credenciales

- Config:
Necesitamos guardar:
#URL base de OrangeHRM
#Credenciales
#Browsers a ejecutar
#Timeouts

Archivos sin carpeta
#.env (para credenciales)
#.gitignore (para que NO suban archivos sensibles a GitHub)
#playwright.config.ts (configuración de Playwright)

.gitignore:
#node_modules/
#.env
#.env.local
#playwright-report/
#test-results/
#.DS_Store
#*.log

.env
((CLAVE=VALOR))
#BASE_URL=https://opensource-demo.orangehrmlive.com
#USERNAME=Admin
#PASSWORD=admin123

playwright.config.ts
#Este archivo configura:
#Browsers (Chrome, Firefox)
#Timeouts
#Reporter (reportes HTML)
#Variables de ambiente

Estructura:
#2 projects (uno Chrome, uno Firefox)
#2 workers (ejecución paralela)
#Cada test corre en los dos browsers automáticamente


#Ahora sí, abre playwright.config.ts y copia esto:
#----------------------
typescriptimport { defineConfig, devices } from '@playwright/test';
import dotenv from 'dotenv';

dotenv.config();

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : 2,
  reporter: 'html',
  use: {
    baseURL: process.env.BASE_URL,
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },

  projects: [
    {
      name: 'chromium',
      use: { ...devices.chromiumDesktop },
    },
    {
      name: 'firefox',
      use: { ...devices.firefox },
    },
  ],
});

#------------------

#Perfecto. Antes de continuar, necesitas instalar dotenv:
npm install -D dotenv
#dotenv: Es una librería que LEE tu archivo .env y lo carga 
#en process.env.

#Abre tu editor, crea el archivo:
tests/pages/base.page.ts

#En base page: 
#En Playwright hay un objeto específico que representa 
#la página del navegador. Se llama Page.
Diferencia:
WDIO: browser (representa el navegador completo)
Playwright: Page (representa UNA pestaña/página del navegador)
#El Page es lo que te permite:
#Clickear elementos
#Escribir texto
#avegar a URLs
#Esperar elementos

import { Page } from '@playwright/test'
#Page es una clase/tipo que Playwright exporta.

void significa "nada" o "sin retorno".

El parámetro { state: 'visible' }
#dice "espera hasta que esté visible".

Page es el objeto del navegador de Playwright. 
BasePage es tu clase.

#<a> que tenga la clase oxd-userdropdown-link 
#Y el atributo role="menuitem""